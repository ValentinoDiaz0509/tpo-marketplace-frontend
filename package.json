{
  "name": "marketplace-front",
  "private": true,
  "scripts": {
    "install-all": "npm --prefix front-usuario install && npm --prefix front-admin install",
    "dev-usuario": "npm --prefix front-usuario run dev",
    "dev-admin": "npm --prefix front-admin run dev",
    "dev-both": "npm-run-all --parallel dev-usuario dev-admin"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
